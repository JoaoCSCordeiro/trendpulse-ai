# Provisiona automaticamente o Prometheus como datasource do Grafana,
# para que não seja preciso configurar nada manualmente na UI depois do
# `docker compose up`.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
